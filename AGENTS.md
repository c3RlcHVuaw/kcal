# Repository instructions

- After making project changes, validate them when practical, then commit and push all repository changes to GitHub automatically.
- Use concise commit messages that describe the completed change.
- If `origin` or an upstream branch is not configured, stop and tell the user exactly which GitHub remote is needed.
- Keep secrets out of git. Do not commit `.env`, virtual environments, caches, logs, or generated build artifacts.
